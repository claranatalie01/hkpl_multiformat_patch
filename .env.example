# Copy this file to .env and replace the secrets before starting the stack.
DB_PASSWORD=replace-with-a-strong-password
ADMIN_API_KEY=replace-with-a-random-admin-key

# Optional integrations and Compose substitutions
PHOENIX_PROJECT_NAME=hkpl-rag
HF_TOKEN=

# Retrieval corpus. Use a fresh name when enabling hybrid retrieval so the
# generated full-text column is created before ingestion.
VECTOR_TABLE=hkpl_knowledge_hybrid
TEXT_SEARCH_CONFIG=english
DENSE_TOP_K=30
LEXICAL_TOP_K=30
FUSION_TOP_K=20
RRF_K=60
RERANK_TOP_N=8
